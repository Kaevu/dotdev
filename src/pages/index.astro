---
// src/pages/index.astro
import BaseLayout from "../layouts/BaseLayout.astro";
import RecentPosts from "../components/RecentPosts.astro";
import RecentGames from "../components/RecentGames.astro";
import { getCollection } from 'astro:content';

const hasPosts = (await getCollection('blog', ({ data }) => !data.draft)).length > 0;
---

<BaseLayout>
  <div class="space-y-9">
    <div>
      <p class="text-neutral-300 text-sm leading-relaxed">
        Kaevu — notes from a CS grad with interests in distributed systems,
        infrastructure, tooling, and baseball data.
      </p>
      <div class="flex flex-wrap gap-2 mt-3">
        <a href="/baseball" class="pill pill-other">baseball</a>
        <a href="/bookmarks" class="pill pill-other">bookmarks</a>
        <a href="/reading" class="pill pill-other">reading</a>
      </div>
    </div>

    <div class="border-l-2 border-neutral-700 pl-4 py-1">
      <p class="text-neutral-400 italic text-sm">
        "A man's reach should exceed his grasp, or what's a heaven for?"
      </p>
    </div>

    {hasPosts && (
      <section aria-labelledby="writing-heading">
        <h2 id="writing-heading" class="text-xl font-light tracking-tight mb-4 font-sans">Writing</h2>
        <RecentPosts />
      </section>
    )}

    <section aria-labelledby="games-heading">
      <h2 id="games-heading" class="text-xl font-light tracking-tight mb-4 font-sans">Recent games</h2>
      <RecentGames />
    </section>
  </div>
</BaseLayout>
